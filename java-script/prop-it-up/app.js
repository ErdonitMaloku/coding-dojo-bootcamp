import react from 'react';

const product = (props) => {
    return (
        <div>
            <h2>{props.title}</h2>
            <p>description:{propd.des} </p>
            <p>cost: ${props.cost}</p>
        </div>
    )
}

export default product